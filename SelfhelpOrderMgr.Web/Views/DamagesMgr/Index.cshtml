
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    @{Html.RenderPartial("~/Views/Shared/_PartialHeadPage.cshtml");}
    <title>Index</title>
    <style>
        .fitem {
            padding-left: 10px;
        }
    </style>
</head>
<body>
    <div>
        <!-- 数据表格容器 -->
        <table id="dg" class="easyui-datagrid" title="基础类型管理"
               style="width:99%;height:500px"
               toolbar="#toolbar" pagination="true"
               rownumbers="true" fitColumns="true">
            <thead>
                <tr>
                    <th field="Id" width="20">Id</th>
                    <th field="TypeName" width="20">类型名称</th>
                    <th field="MaxUseMoney" width="15" formatter="formatCurrency">最大金额</th>
                    <th field="RetentionRate" width="15" formatter="formatPercentage">留存率</th>
                    <th field="Remark" width="25">备注</th>
                    <th field="CrtBy" width="20">创建人</th>
                    <th field="CreateDate" width="20" formatter="formatDateTime">创建时间</th>
                    <th field="ModBy" width="20">最后修改人</th>
                    <th field="ModifyDate" width="20" formatter="formatDateTime">最后修改时间</th>
                </tr>
            </thead>
        </table>

        <!-- 工具栏 -->
        <div id="toolbar">
            <a href="javascript:void(0)" class="easyui-linkbutton" iconCls="icon-add" onclick="newItem()">新建</a>
            <a href="javascript:void(0)" class="easyui-linkbutton" iconCls="icon-edit" onclick="editItem()">编辑</a>
            <a href="javascript:void(0)" class="easyui-linkbutton" iconCls="icon-remove" onclick="destroyItem()">删除</a>
            <input id="searchbox" class="easyui-searchbox" style="width:200px" prompt="搜索..." />
        </div>

        <!-- 编辑对话框 -->
        <div id="dlg" class="easyui-dialog" style="width:400px" closed="true" buttons="#dlg-buttons">
            <form id="fm" method="post">
                <div class="fitem">
                    <label>类型名称:</label>
                    <input name="TypeName" class="easyui-validatebox" required="true" />
                </div>
                <div class="fitem">
                    <label>最大金额:</label>
                    <input name="MaxUseMoney" class="easyui-numberbox" precision="2" required="true" />
                </div>
                <div class="fitem">
                    <label>留存率(%):</label>
                    <input name="RetentionRate" class="easyui-numberspinner" min="0" max="100" required="true" />
                </div>
                <div class="fitem">
                    <label>备注:</label>
                    <input name="Remark" class="easyui-textbox" multiline="true" style="height:60px" />
                </div>
                <input type="hidden" name="Id" />
            </form>
        </div>

        <!-- 对话框按钮 -->
        <div id="dlg-buttons">
            <a href="javascript:void(0)" class="easyui-linkbutton" iconCls="icon-ok" onclick="saveItem()">保存</a>
            <a href="javascript:void(0)" class="easyui-linkbutton" iconCls="icon-cancel" onclick="javascript:$('#dlg').dialog('close')">取消</a>
        </div>

        <script>
            // 初始化数据网格
            $('#dg').datagrid({
                url: '/DamagesMgr/GetBaseTypeList',
                method: 'POST',
                singleSelect: true
            });

            // 格式化方法
            function formatCurrency(val) {
                return '¥' + (val ? val.toFixed(2) : '0.00');
            }

            function formatPercentage(val) {
                return (val || 0) + '%';
            }

            function formatDateTime(val) {
                return val ? new Date(val).toLocaleString() : '';
            }

            // CRUD操作函数
            function newItem() {
                $('#dlg').dialog('open').dialog('setTitle', '新建');
                $('#fm').form('clear');
            }

            function editItem() {
                const row = $('#dg').datagrid('getSelected');
                if (row) {
                    $('#dlg').dialog('open').dialog('setTitle', '编辑');
                    $('#fm').form('load', row);
                }
            }

            function saveItem() {
                $('#fm').form('submit', {
                    url: '/DamagesMgr/BaseTypeSave',
                    onSubmit: function () {
                        return $(this).form('validate');
                    },
                    success: function (res) {
                        const result = JSON.parse(res);
                        if (result.Flag) {
                            $('#dlg').dialog('close');
                            $('#dg').datagrid('reload');
                        } else {
                            $.messager.alert('错误', result.ReMsg);
                        }
                    }
                });
            }

            function destroyItem() {
                const row = $('#dg').datagrid('getSelected');
                if (row) {
                    $.messager.confirm('确认', '确定要删除该记录吗？', function (r) {
                        if (r) {
                            $.post('/DamagesMgr/BaseTypeDelete', { id: row.Id }, function (res) {
                                if (res.Flag) {
                                    $('#dg').datagrid('reload');
                                } else {
                                    $.messager.alert('错误', res.ReMsg);
                                }
                            });
                        }
                    });
                }
            }
        </script>

        <style>
            .fitem label {
                display: inline-block;
                width: 80px;
                margin: 10px 15px 10px 0;
            }
        </style>

    </div>
</body>
</html>
